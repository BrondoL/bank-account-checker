export interface CheckAccountFormType {
    accountBank: string;
    accountNumber: string;
}

export interface BankAccountType {
    accountBank: string;
    accountNumber: string;
    accountName: string;
}